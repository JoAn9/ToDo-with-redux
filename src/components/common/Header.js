import React, {PropTypes} from 'react';

const Header = () => {
  return (
    <header>
      <div>
        Application TO DO<br/>with a surprise
      </div>
      <div>
        React, Redux and React Router in ES6. Also Fetch, Bootstrap and other fancy things.
      </div>
    </header>
  );
};

export default Header;
